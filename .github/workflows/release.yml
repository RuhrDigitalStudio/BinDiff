name: release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  package:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 8.0.x
      - run: dotnet restore BinDiff.slnx
      - run: dotnet test BinDiff.Tests/BinDiff.Tests.csproj -c Release --no-restore
      - name: Publish Windows applications
        shell: pwsh
        run: |
          dotnet publish BinDiff.Cli/BinDiff.Cli.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:DebugType=None -o artifacts/cli
          dotnet publish BinDiff.Gui/BinDiff.Gui.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:DebugType=None -o artifacts/gui
          Compress-Archive -Path artifacts/cli/* -DestinationPath artifacts/bindiff-cli-win-x64.zip
          Compress-Archive -Path artifacts/gui/* -DestinationPath artifacts/bindiff-gui-win-x64.zip
          Get-FileHash artifacts/*.zip -Algorithm SHA256 |
            ForEach-Object { "{0}  {1}" -f $_.Hash.ToLowerInvariant(), (Split-Path $_.Path -Leaf) } |
            Set-Content artifacts/SHA256SUMS.txt -Encoding ascii
      - name: Create GitHub release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "${{ github.ref_name }}" artifacts/*.zip artifacts/SHA256SUMS.txt --verify-tag --generate-notes --title "BinDiff ${{ github.ref_name }}"
